/**
 * Sponsor images shown on the home page.
 * All images in public/images/sponsors are included automatically at build time.
 * Run `node scripts/generate-sponsors-list.js` to regenerate, or `npm run build` (runs it automatically).
 * To add custom alt/link per image, edit src/config/sponsors.generated.ts after generating.
 */
export { SPONSOR_IMAGES } from "./sponsors.generated";
